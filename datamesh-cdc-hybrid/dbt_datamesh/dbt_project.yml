name: hybrid_cdc
version: 1.0.0
config-version: 2

profile: hybrid_postgres

model-paths: ["models"]
test-paths: ["tests"]
target-path: target
clean-targets: [target, dbt_packages]

models:
  hybrid_cdc:
    staging:
      +materialized: view
      +schema: staging
    gold:
      +materialized: table
      +schema: gold
    governance:
      +materialized: view
      +schema: governance_mart
