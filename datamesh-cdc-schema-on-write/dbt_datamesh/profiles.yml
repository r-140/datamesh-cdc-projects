datamesh_trino:
  target: dev
  outputs:
    dev:
      type: trino
      method: none  # no auth for local demo
      user: admin
      host: localhost
      port: 8080
      database: iceberg
      schema: raw
      threads: 4
